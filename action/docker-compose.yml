
services:
  postgres:
    image: postgres:16-alpine
    container_name: mergehelpagent-db
    environment:
      POSTGRES_DB: mergehelpagent
      POSTGRES_USER: mergehelpagent
      POSTGRES_PASSWORD: mergehelpagent
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mergehelpagent"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata: